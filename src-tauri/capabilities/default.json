{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local fallback page",
  "windows": ["main"],
  "permissions": ["core:default"]
}
